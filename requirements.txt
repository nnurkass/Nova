# ===================================================================
# Nova — Requirements
# Python 3.11+
# ===================================================================

# --- LangChain / LangGraph core stack ---
langchain>=0.3.0,<2.0.0
langgraph>=0.2.0,<2.0.0
langgraph-supervisor>=0.0.4,<1.0.0
langchain-anthropic>=0.3.0,<2.0.0

# --- Monitoring / tracing ---
langsmith>=0.1.0,<1.0.0

# --- API server ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# --- Database ---
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0

# --- Cache / queues ---
redis>=5.0.0,<6.0.0

# --- Configuration ---
pydantic-settings>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# --- HTTP client ---
httpx>=0.27.0,<1.0.0

# --- Web scraping ---
beautifulsoup4>=4.12.0,<5.0.0
# lxml>=5.0.0,<6.0.0  # optional fast parser; uses built-in html.parser if not installed

# --- Document parsing ---
xmltodict>=0.13.0,<1.0.0
openpyxl>=3.1.0,<4.0.0
# Note: PyPDF2 is deprecated upstream; planned migration to `pypdf` in v0.2
PyPDF2>=3.0.0,<4.0.0

# --- Testing ---
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=4.0.0,<7.0.0
